#include "main.h"

/**
 * print_square - prints square
 * @size: parameter
 * Return: Always 0 (Success)
 */
void print_square(int size)
{
	int i;
	int j;

	if (n > 0)
	{
	for (j = 0 ; j < n ; j++)
	{
	for (i = 0 ; i < n ; i++)
	_putchar('#');
	_putchar('\n');
	}}
	else
	_putchar('\n');
}
