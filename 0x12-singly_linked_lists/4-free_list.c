#include <stdio.h>
#include "lists.h"

/**
 * free_list - function
 * @head: parameter
 *
 * Description: prints all elements
 * Return: number of nodes
 */
void free_list(list_t *head)
{
list_t *X;

X = head;
head = head->next;
free(x);
}

