#include "main.h"

/**
 * _strchr - filling memo with a const byet
 * @s: parameter
 * @c: parameter
 * Return: Always 0 (Success)
 */
char *_strchr(char *s, char c)
{
char *m;
char f;

m = s;
f = c;

return (m);
}
