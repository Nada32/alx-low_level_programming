#include "main.h"

/**
 * _strcat - write string
 * @dest: parameter
 * @src: parameter
 * Return: Always 0 (Success)
 */
char *_strcat(char *dest, char *src)
{
	int i;

	i = 0;
	while (str[i] != '\0')
	{
	_putchar(str[i]);
	i = i + 1;
	}
	_putchar('\n');
}


