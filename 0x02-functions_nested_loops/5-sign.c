#include "main.h"

/**
 *  print_sign - prints the sign of a num .
 * @n: parameter any char
 * Return: Always 0.
 */

int print_sign(int n)
{
	char a = 'A';
	char z = 'z';
	int i = (int)(a);
	int j = (int)(z);

	if (c > 0)
	{
	_putchar('+');
	return (1);
	}
	else if (c < 0)
	{
	_putchar('-');
	return (-1);
	}
	else if (c == 0)
	{
	_putchar('0');
	return (0);
	
	}

}
